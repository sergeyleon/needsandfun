index_page:
  pattern: -page-{page}
  defaults:
    page: 1
    controller: Places:index
  requirements:
    page: (\d+|all)

index:
  pattern: 
  defaults:
    page: 1  
    controller: Places:index

category_page:
  pattern: -{category}-page-{page}
  defaults:
    page: 1
    controller: Places:category
  requirements:
    page: (\d+|all)

category:
  pattern: -{category}
  defaults:
    controller: Places:category